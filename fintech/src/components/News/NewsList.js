import React from "react";

const NewsList = ({ newList }) => {
  return (
    <div>
      {/* {newList.map((el) => {
        return (
          <>
            <p>이름 : {el.username}</p>
            <p>나이 : {el.age}</p>
            <p>회원키 : {el.height}</p>
          </>
        );
      })} */}
    </div>
  );
};

export default NewsList;
